<form method="post" action="{{ path('app_admin_promo_delete', {'id': promo.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette promo ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ promo.id) }}">
<button class="btn btn-red"><i class="align-middle" data-feather="trash"></i></button>
</form>
