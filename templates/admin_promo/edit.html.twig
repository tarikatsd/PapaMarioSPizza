{% extends 'base-backend.html.twig' %}

{% block title %}Modifier Promo{% endblock %}

{% block body %}


    <a href="{{ path('app_admin_promo_index') }}" class="btn btn-orange btn-sm mb-2">
        Retour a la liste
    </a>
    <div class="d-flex justify-content-between align-items-center">
<h1>Modifier Promo</h1>
    {{ include('admin_promo/_delete_form.html.twig') }}

</div>
    {{ include('admin_promo/_form.html.twig', {'button_label': 'Modifier'}) }}

{% endblock %}
