<form method="post" action="{{ path('app_admin_ingredient_delete', {'id': ingredient.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir suprimée?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingredient.id) }}">
<button class="btn btn-danger"><i class="align-middle" data-feather="trash"></i></button>
</form>

