{% extends 'base-backend.html.twig' %}

{% block title %}
    Modifier Ingrédients
{% endblock %}

{% block body %}
<a href="{{ path('app_admin_ingredient_index') }}" class="btn btn-orange btn-sm mb-2">Retour a la liste</a>
<div class="d-flex justify-content-between align-items-center">
<h1>Modifier Ingrédients</h1>
    <a class="btn btn-brown" href="{{ path('app_admin_ingredient_edit',{'id': ingredient.id}) }}">
    Modifié 
</a></div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ingredient.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ingredient.nom }}</td>
            </tr>
            <tr>
                <th>Activée
                <td>{{ ingredient.isActive ? 'Oui' : 'Non' }}</td>
        </tbody>
    </table>
    {{ include('admin_ingredient/_delete_form.html.twig') }}
{% endblock %}
