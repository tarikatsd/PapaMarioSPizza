<form method="post" action="{{ path('app_admin_dessert_delete', {'id': dessert.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer ce dessert ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dessert.id) }}">
<button class="btn btn-red"><i class="align-middle" data-feather="trash"></i></button>
</form>
