<form method="post" action="{{ path('app_admin_cgv_delete', {'id': cgv.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir suprimée?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cgv.id) }}">
<button class="btn btn-red"><i class="align-middle" data-feather="trash"></i></button>
</form>
