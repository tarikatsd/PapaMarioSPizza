{% extends 'base-backend.html.twig' %}
{% block title %}Conditions Générales de Vente{% endblock %}

{% block body %}
<a href="{{ path('app_admin_cgv_index') }}" class="btn btn-orange btn-sm mb-2">Retour a la liste</a>
<div class="d-flex justify-content-between align-items-center">
<h1>Conditions Générales de Vente</h1>
<a class="btn btn-brown" href="{{ path('app_admin_cgv_edit', {'id': cgv.id}) }}">Modifiée</a>
</div>

    <table class="table">
        <tbody>
            <tr>
                <th>Texte</th>
                <td>{{ cgv.texte }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('admin_cgv/_delete_form.html.twig') }}
{% endblock %}
