{% extends 'base.html.twig' %}

{% block title %}Reinitialisez votre mot de passe{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}
    <h1>Reinitialisez votre mot de passe</h1>

    {{ form_start(requestForm) }}
        {{ form_row(requestForm.email) }}
        <div>
            <small>
                Entre votre adresse email et nous vous enverrons un lien pour reinitialiser votre mot de passe.
            </small>
        </div>

        <button class="btn btn-brown">Envoyer l'email</button>
    {{ form_end(requestForm) }}
{% endblock %}
