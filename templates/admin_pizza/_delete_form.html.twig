<form method="post" action="{{ path('app_admin_pizza_delete', {'id': pizza.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette pizza ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pizza.id) }}">
<button class="btn btn-red"><i class="align-middle" data-feather="trash"></i></button>
</form>
